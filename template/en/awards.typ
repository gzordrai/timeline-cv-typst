#import "../../timeline-cv.typ": *
#let metadata = toml("../metadata.toml")
#let entry_side = entry_side.with(data: metadata)

#section_items(
  title: "Awards",
  (
    entry_side(
      title: [Turing Award],
      preposition: [for having such a wonderful name],
      where: [],
      when: [],
    )[],
    entry_side(
      title: [Best drawing],
      preposition: [of],
      where: [My Kindergarten.],
      when: [],
    )[Prize of one (1) green sticker.],
  ),
)
